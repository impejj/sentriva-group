name: SENTRIVA Responsive Visual Gate

on:
  pull_request:
    paths:
      - "*.html"
      - "*.css"
      - "*.js"
      - "qa/**"
      - ".github/workflows/responsive-visual-gate.yml"
  push:
    branches:
      - feat/sentriva-v1-evolution
    paths:
      - "*.html"
      - "*.css"
      - "*.js"
      - "qa/**"
      - ".github/workflows/responsive-visual-gate.yml"

permissions:
  contents: read

jobs:
  responsive-visual:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install Playwright
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Start static server
        run: |
          python3 -m http.server 4173 --bind 127.0.0.1 > /tmp/sentriva-http.log 2>&1 &
          echo $! > /tmp/sentriva-http.pid
          sleep 1
      - name: Run responsive browser gate
        run: node qa/responsive-smoke.mjs
      - name: Upload responsive evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sentriva-responsive-evidence
          path: qa/artifacts/
          if-no-files-found: warn
          retention-days: 14
