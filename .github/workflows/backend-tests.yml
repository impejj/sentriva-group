name: SENTRIVA Backend Tests
# EXECUTION_POLICY: GitHub-hosted automatic execution suspended; explicit manual dispatch only.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt

      - name: Install dependencies
        run: pip install -r backend/requirements-dev.txt

      - name: Run backend + frontend submission contract tests
        env:
          PYTHONPATH: .
        run: pytest -q backend/tests
