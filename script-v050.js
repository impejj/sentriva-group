const toggle=document.querySelector('.nav-toggle');const nav=document.querySelector('.nav');if(toggle&&nav){toggle.addEventListener('click',()=>{const open=nav.classList.toggle('is-open');toggle.setAttribute('aria-expanded',String(open));});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('is-open');toggle.setAttribute('aria-expanded','false');}));}const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();const fieldLabel=field=>{const label=field.closest('label');if(!label)return field.name.replaceAll('_',' ');const clone=label.cloneNode(true);clone.querySelectorAll('input,textarea,select').forEach(node=>node.remove());return clone.textContent.trim()||field.name.replaceAll('_',' ');};document.querySelectorAll('[data-email-form]').forEach(form=>{form.addEventListener('submit',event=>{event.preventDefault();if(!form.reportValidity())return;const recipient=form.dataset.recipient;const subject=form.dataset.subject||'Sentriva Group';const lines=[];form.querySelectorAll('fieldset').forEach(fieldset=>{const legend=fieldset.querySelector('legend');if(legend)lines.push(legend.textContent.trim());fieldset.querySelectorAll('input,textarea,select').forEach(field=>{if(!field.name||field.type==='file'||field.type==='submit')return;const value=String(field.value||'').trim();if(value)lines.push(`${fieldLabel(field)}: ${value}`);});lines.push('');});lines.push('Documentación: adjuntar al correo si corresponde.');window.location.href=`mailto:${recipient}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(lines.join('\n'))}`;});});